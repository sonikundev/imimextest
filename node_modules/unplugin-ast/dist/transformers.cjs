"use strict";Object.defineProperty(exports, "__esModule", {value: true});// src/core/transformers/remove-wrapper-function.ts
var _utils = require('@antfu/utils');
var RemoveWrapperFunction = (functionNames) => ({
  onNode: (node) => node.type === "CallExpression" && node.callee.type === "Identifier" && _utils.toArray.call(void 0, functionNames).includes(node.callee.name),
  transform(node) {
    return node.arguments[0];
  }
});


exports.RemoveWrapperFunction = RemoveWrapperFunction;
