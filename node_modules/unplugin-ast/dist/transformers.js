// src/core/transformers/remove-wrapper-function.ts
import { toArray } from "@antfu/utils";
var RemoveWrapperFunction = (functionNames) => ({
  onNode: (node) => node.type === "CallExpression" && node.callee.type === "Identifier" && toArray(functionNames).includes(node.callee.name),
  transform(node) {
    return node.arguments[0];
  }
});
export {
  RemoveWrapperFunction
};
